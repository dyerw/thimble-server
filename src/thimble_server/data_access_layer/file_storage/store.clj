;; TODO: fns to:
;;             generate safe filename
;;             save to s3
;;             take audio and call other fns and return filename [or something else]

;; just returns static string at the moment for testing other logics
(defn store-audio!
  "takes audio, stores to s3 returns filename-key"
  [audio]
  "testname")
